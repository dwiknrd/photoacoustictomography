{
  "name": "photoaccoustic",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w yourmodule",
    "package-linux": "electron-packager . Photoacoustictomography --overwrite --asar=true --platform=linux --arch=x64 --icon=images/favicon.png --prune=true --out=release-builds",
    "package-rasp": "electron-packager . Photoacoustictomography --overwrite --asar=true --platform=linux --arch=arm64 --icon=images/favicon.png --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^9.4.0",
    "serialport": "^8.0.7"
  },
  "devDependencies": {
    "electron-packager": "^15.1.0",
    "electron-rebuild": "^1.11.0"
  }
}
